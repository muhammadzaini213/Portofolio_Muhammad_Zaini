/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "github.com",
      "user-attachments.githubusercontent.com"
    ],
  },
};

module.exports = nextConfig;